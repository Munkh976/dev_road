# ---- core numerics -------------------------------------------------------
pandas>=2.2
numpy>=1.26
pyarrow>=16.0          # parquet engine for the price cache

# ---- config --------------------------------------------------------------
PyYAML>=6.0
pydantic>=2.7          # typed config; fails loudly on a bad config.yaml
python-dotenv>=1.0     # secrets from .env, never from source

# ---- broker --------------------------------------------------------------
ib_async>=1.0          # maintained fork of ib_insync (archived 2024)

# ---- operational store + approval UI ------------------------------------
sqlite-utils>=3.36
datasette>=0.65.5,<1.0   # patched 0.65.x; see docs/datasette_patterns.md

# ---- backtesting ---------------------------------------------------------
vectorbt>=0.26

# ---- reporting -----------------------------------------------------------
Jinja2>=3.1

# ---- AI veto layer -------------------------------------------------------
anthropic>=0.40

# ---- scheduling ----------------------------------------------------------
APScheduler>=3.10
