# ---- secrets: never commit -----------------------------------------------
.env
*.key

# ---- generated data ------------------------------------------------------
data/cache/*.parquet
data/cache/_manifest.json
db/*.db
db/*.db-wal
db/*.db-shm
reports/*.html
backtest_results/
# Temporary reference copy — never commit, will be deleted
resette_cloud/

# journal entries are yours; keep the directory, ignore the contents
journal/*.md
!journal/TEMPLATE.md

# ---- python --------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# ---- os ------------------------------------------------------------------
.DS_Store
